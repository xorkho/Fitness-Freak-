import { BrowserRouter as Router } from "react-router-dom";
import AppWrapper from "./AppWrapper"; // import the new file

function App() {
    return (
        <Router>
            <AppWrapper />
        </Router>
    );
}

export default App;
